# ⚠️ 本文件对当前构建**不生效**：release.sh 对 me 的构建上下文是工作区根
#    （svc_context → "."），Docker 只读上下文根的 .dockerignore。
#    实际生效的是同目录的 Dockerfile.dockerignore，本文件是它的**语义镜像**
#    （按「上下文＝本仓根」的形态改写），仅为「有人改用本仓为上下文构建」时兜底。
#
# ⚠️ 一律带 `**/` 前缀：不带前缀的模式只匹配**上下文根**（Docker 的 dockerignore
#    不会像 gitignore 那样自动递归到任意深度），而实际产物在 src/PandaAuth.Me/bin|obj。
#
# 规则清单与 webadmin / website 的 .dockerignore 保持同一套。
**/bin/
**/obj/
**/TestResults/
**/node_modules/
# frontend/dist/ 见 .gitignore；今天 vite 的 outDir 已指向 wwwroot/me，此条属空转。
**/dist/
.git/
.vs/
.vscode/
.idea/
.zcode/
.superpowers/

# 敏感文件：本仓 .gitignore 早已声明这些路径，为与 webadmin / website 那两份
# （在各自仓根上下文下**实际生效**）保持同一套策略而补齐。注意本文件对当前构建
# 不生效，真正生效的是 Dockerfile.dockerignore —— 那份也已有同样的规则。
**/*.user
**/appsettings.Local.json
**/appsettings.*.Local.json
**/.env
**/*.env

# 本仓无 deploy/ 目录，这条属预防性规则（与 me / server 那份同一清单）。
**/deploy/backups/

# 原基线里的 `*.md` 已删除：不带 `**/` 的模式只匹配**上下文根**，会挡掉随源码提交的
# 根级文档（README.md 等）。排除 .md 本就无收益，且 Dockerfile.dockerignore 一直
# 刻意不排 *.md，两份保持一致。

# 前端构建产物（由 vite 输出，不入库）。
# 不变式：git ls-files src/PandaAuth.Me/wwwroot 命中数为 0，
# 该目录 100% 是构建输出。若将来这里混入**随源码提交**的资产
# （对照 server / website 的 wwwroot/brand/**），必须收窄为只排除
# src/PandaAuth.Me/wwwroot/me/，否则会把该资产挡在镜像外。
src/PandaAuth.Me/wwwroot/
